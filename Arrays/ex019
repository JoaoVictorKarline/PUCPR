vet = [1, 0, 0, 0, 1, 1, 0, 0, 0, 0, 1]
sum = 0
maior = 0

for c in range(0, len(vet)):
    if vet[c] == 0:
        sum += 1
    else:
        if sum > maior:
            maior = sum
        sum = 0

if sum > maior:
    maior = sum

print(maior)
