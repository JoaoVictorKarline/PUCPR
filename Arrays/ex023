texto = input()
str_upper = ""
str_lower = ""

for c in texto:
    if 97 <= ord(c) <= 122:
        str_upper += chr(ord(c) - 32)
    else:
        str_upper += c

    if 65 <= ord(c) <= 90:
        str_lower += chr(ord(c) + 32)
    else:
        str_lower += c

print(str_upper)
print(str_lower)
