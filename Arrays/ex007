vetor = [i+1 for i in range(100)]
soma = 0

for i in range(50):
    soma += vetor[i] + vetor[99-i]

print("A soma dos elementos opostos do vetor é:", soma)

# Solução que utilizei
vetor = [1, 5, 7, 8, 5, 5] 
soma = 0

for i in range(1, len(vetor)-1):
    soma = vetor[i-1] + vetor[len(vetor)-i]
    print(soma)
