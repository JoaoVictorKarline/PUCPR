import random

while True:
    num = int(input("Informe um valor aleatório entre 1 e 100: "))
    acerto = random.randint(1, 10)
    if num == acerto:
        print("Você acertou!")
        break
    else:
        print(f"O número escolhido foi {acerto}")
        continue
